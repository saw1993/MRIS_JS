const getPermissionsForRole = require('../usecases/role/getPermissionsForRole');

const permissionRepository = {
  getPermissionsForRole
};

module.exports = permissionRepository; // Make sure this line is correct!